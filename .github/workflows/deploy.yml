name: wengo
on:
  pull_request: 
    branches: [main]
jobs:
  build:
  runs-on: ubuntu-latest
steps:
  — uses: actions/checkout@v2
  — name: Use Node.js
    uses: actions/setup-node@v1
    with:
      node-version: 14.x
  - name: Install dependencies
    run: npm install
  - name: Run unit tests
    run: npm test
  - name: Build
    run: npm run build

  name: Deploy to Skynet
  uses: kwypchlo/deploy-to-skynet-action@main
  with:
    upload-dir: build
    github-token: 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
